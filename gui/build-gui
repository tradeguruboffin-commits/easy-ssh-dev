#!/bin/bash
# build-py.sh - PyInstaller script equivalent

echo "🚀 Building Python binary..."

# Single file optimized
pyinstaller \
    --noconfirm \
    --onefile \
    --name sshx-gui \
    --add-data "lib/git-auth:lib" \
    --add-data "lib/sshx-reset:lib" \
    esey-ssh-gui.py
    
echo "✅ Build complete! dist/sshx-gui ready"
echo "💡 Test: ./dist/sshx-gui"
