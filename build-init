#!/usr/bin/env bash
set -euo pipefail

APP_NAME="sshx-dev"
SOURCE_FILE="init.go"

OUTPUT_BIN="$APP_NAME"

echo "🔨 Building $APP_NAME..."

go build -o "$OUTPUT_BIN" "$SOURCE_FILE"

chmod +x "$OUTPUT_BIN"

echo "✅ Build complete!"
echo "📦 Output: $OUTPUT_BIN"
